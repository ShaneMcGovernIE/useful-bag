-- Sharing metadata, read by tooling and the manager detail pane only.
return {
  summary = "Bag holds 999 distinct item slots instead of 20.",
  author = "Pokemon Gen 1 Recompilation Project",
  contact = "https://github.com/bryanthaboi/gen1recomp",
  tags = { "data-only", "beginner", "quality-of-life" },
  differences = {
    changed = { "bag capacity 20 -> 999 distinct item slots" },
    added = {
      "TAB / R3 while the bag is open sorts it by name or by count",
    },
    known = {
      "the per-slot quantity cap of 99 is unchanged",
      "legacy Gen 1 save imports still read at most 20 bag items; the cap applies to fresh native saves",
    },
  },
  credits = {},
  compat = { engine = ">=1.0.0 <2.0.0", modApi = 2 },
}
